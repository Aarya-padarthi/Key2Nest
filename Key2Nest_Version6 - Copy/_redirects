# Key2Nest digital business cards — clean per-person URLs.
# Rewrites (200) keep the pretty URL in the address bar while serving the
# card page from /cards/<slug>/. The card pages use absolute /cards/ asset
# paths so they load correctly under the rewritten URL.
# Managed alongside cards/build.py — keep in sync if slugs change.
/vivek       /cards/vivek/index.html      200
/naveen      /cards/naveen/index.html     200
/sreedhar    /cards/sreedhar/index.html   200
/lakshmi     /cards/lakshmi/index.html    200
/rj          /cards/rj/index.html         200
